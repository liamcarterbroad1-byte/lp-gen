# Optional. If set, the server uses this key as a fallback when a user hasn't
# pasted their own Claude key in the dashboard. Leave unset to require each user
# to bring their own key (recommended).
# ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
# PORT=3000
# LP_GEN_MODEL=claude-opus-4-8
