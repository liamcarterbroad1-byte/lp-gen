{
  "name": "lp-gen",
  "version": "1.0.0",
  "description": "Dashboard that turns a client brief into a paste-ready GoHighLevel landing page using the user's own Claude.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.68.0",
    "express": "^4.21.2"
  }
}
